{
  "feature_id": "containers-ux-simplification-r1-2026-02-15",
  "title": "Container management progressive disclosure UX",
  "status": "in_review",
  "acceptance_criteria": [
    "System -> Containers keeps all existing create/edit functionality.",
    "Optional/advanced sections are collapsed by default and expandable on demand.",
    "Template action clearly indicates it only loads values and does not install.",
    "LAN helper is optional, collapsible, and keeps description-first interface labels.",
    "Frontend validation gates pass (tsc, lint, build, runtime smoke, UI smoke)."
  ],
  "assumptions": [
    "Reducing default visible form density improves ease of use without removing capability.",
    "This is a frontend-only change and must keep backend/API contracts unchanged.",
    "Existing unrelated worktree changes remain untouched."
  ],
  "tasks": [
    {
      "id": "T1",
      "owner_role": "UI",
      "status": "done",
      "files": [
        "frontend/src/app/system/containers/page.tsx"
      ],
      "notes": "Implemented collapsible advanced sections and clearer template/helper UX."
    },
    {
      "id": "T2",
      "owner_role": "Test",
      "status": "done",
      "files": [],
      "notes": "Executed frontend tsc/lint/build + runtime/browser smoke checks."
    },
    {
      "id": "T3",
      "owner_role": "Reviewer",
      "status": "doing",
      "files": [
        "frontend/src/app/system/containers/page.tsx"
      ],
      "notes": "Manual review pending against UX goals in live UI."
    }
  ],
  "commands": {
    "build": "cd frontend && npm run -s build",
    "test": "cd frontend && npx tsc --noEmit --pretty false && SMOKE_BASE_URL='http://localhost:3000' npm run -s smoke:runtime && LD_LIBRARY_PATH=/home/redhot/VyOS/.local-playwright-libs/extracted/usr/lib/x86_64-linux-gnu SMOKE_BASE_URL='http://localhost:3000' npm run -s smoke:ui",
    "lint": "cd frontend && npm run -s lint"
  },
  "worktrees_or_threads": [
    {
      "name": "feature/containers-automation-v1",
      "purpose": "Container management UX simplification",
      "status": "active"
    }
  ]
}
