{
  "feature_id": "traffic-policy-qos-default-subtree-2026-02-15",
  "title": "Traffic Policy parity deepening (QoS default subtree + traffic-match-group)",
  "status": "done",
  "acceptance_criteria": [
    "`/network/traffic-policy` provides form-driven CRUD for `qos traffic-match-group` entries.",
    "`/network/traffic-policy` QoS policy editor supports `default bandwidth/burst/ceiling/priority/queue-type` fields.",
    "Save path emits scoped diff-based commands for both subtrees while preserving existing policy/class/interface logic.",
    "Validation passes: frontend tsc/build, runtime smoke, and browser smoke."
  ],
  "assumptions": [
    "Existing `/vyos/qos` wrapper endpoint supports all required command roots for this slice.",
    "No backend API contract changes are needed.",
    "Unrelated dirty files in this worktree remain untouched."
  ],
  "tasks": [
    {
      "id": "T1",
      "owner_role": "UI",
      "status": "done",
      "files": [
        "frontend/src/app/network/traffic-policy/page.tsx"
      ],
      "notes": "Added traffic-match-group editor and default subtree fields with parse/state/save integration."
    },
    {
      "id": "T2",
      "owner_role": "Backend",
      "status": "done",
      "files": [
        "backend/routers/qos/qos.py"
      ],
      "notes": "No backend changes required; existing qos wrapper remained sufficient."
    },
    {
      "id": "T3",
      "owner_role": "Test",
      "status": "done",
      "files": [
        "frontend/src/app/network/traffic-policy/page.tsx"
      ],
      "notes": "Validated with frontend tsc/build plus runtime and browser smoke checks."
    },
    {
      "id": "T4",
      "owner_role": "Reviewer",
      "status": "done",
      "files": [
        "frontend/src/app/network/traffic-policy/page.tsx"
      ],
      "notes": "Manual in-session review completed; no blocking issues found."
    }
  ],
  "commands": {
    "build": "cd frontend && npm run -s build",
    "test": "cd frontend && npx tsc --noEmit --pretty false && SMOKE_BASE_URL='http://localhost:3000' npm run -s smoke:runtime && LD_LIBRARY_PATH=/home/redhot/VyOS/.local-playwright-libs/extracted/usr/lib/x86_64-linux-gnu SMOKE_BASE_URL='http://localhost:3000' npm run -s smoke:ui",
    "lint": "cd frontend && npm run -s lint"
  },
  "worktrees_or_threads": [
    {
      "name": "feature/containers-automation-v1",
      "purpose": "Parity implementation and runtime validation",
      "status": "active"
    }
  ]
}
