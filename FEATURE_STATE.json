{
  "feature_id": "none",
  "title": "No active feature",
  "status": "done",
  "acceptance_criteria": [
    "Internal memory files reflect canonical continuation branch as dev.",
    "Feature/containers-automation-v1 branch retirement is documented and ready for manual execution in an unrestricted shell.",
    "Next build startup commands and file handoff points are documented."
  ],
  "assumptions": [
    "Future implementation work will continue directly on dev unless Mark asks for a dedicated feature branch.",
    "Current non-doc dirty files are intentional in-progress work and should remain untouched."
  ],
  "tasks": [
    {
      "id": "T1",
      "owner_role": "Planner",
      "status": "done",
      "files": [
        "PROJECT_MEMORY.md",
        "CURRENT_FEATURE.md",
        "FEATURE_STATE.json",
        "DECISIONS.md",
        "ORCHESTRATOR.md"
      ],
      "notes": "Refreshed durable memory for next-build kickoff and branch policy."
    },
    {
      "id": "T2",
      "owner_role": "Backend",
      "status": "done",
      "files": [],
      "notes": "Confirmed dev as active branch; documented manual follow-up to remove feature/containers-automation-v1 because branch deletion is blocked by command policy in this environment."
    }
  ],
  "commands": {
    "build": "cd frontend && npm run -s build",
    "test": "cd backend && PYTHONPATH=. ./.venv/bin/pytest -q && cd ../frontend && npx tsc --noEmit --pretty false && npm run -s build && npm run -s smoke:runtime",
    "lint": "cd frontend && npm run -s lint"
  },
  "worktrees_or_threads": [
    {
      "name": "dev",
      "purpose": "Canonical continuation branch",
      "status": "active"
    }
  ]
}
