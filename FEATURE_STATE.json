{
  "feature_id": "traffic-policy-qos-traffic-match-group-2026-02-15",
  "title": "Traffic Policy parity deepening (QoS traffic-match-group)",
  "status": "done",
  "acceptance_criteria": [
    "`/network/traffic-policy` provides form-driven CRUD for `qos traffic-match-group` with name, match list, and nested match-group list.",
    "Save path emits scoped diff-based `set/delete qos traffic-match-group ...` commands.",
    "Existing traffic-policy, qos policy/class, and qos interface assignment behavior remains intact.",
    "Validation passes: frontend tsc/lint/build, runtime smoke, and browser smoke."
  ],
  "assumptions": [
    "Existing `/vyos/qos` config wrapper remains sufficient for traffic-match-group operations.",
    "No backend API contract changes are needed for this slice.",
    "Unrelated dirty files in this worktree remain untouched."
  ],
  "tasks": [
    {
      "id": "T1",
      "owner_role": "UI",
      "status": "done",
      "files": [
        "frontend/src/app/network/traffic-policy/page.tsx"
      ],
      "notes": "Added state parsing, editor UI, and diff-based save logic for qos traffic-match-group."
    },
    {
      "id": "T2",
      "owner_role": "Backend",
      "status": "done",
      "files": [
        "backend/routers/qos/qos.py"
      ],
      "notes": "No backend changes required; existing qos wrapper already scopes command execution correctly."
    },
    {
      "id": "T3",
      "owner_role": "Test",
      "status": "done",
      "files": [
        "frontend/src/app/network/traffic-policy/page.tsx"
      ],
      "notes": "Validated with frontend tsc/lint/build plus runtime and browser smoke checks."
    },
    {
      "id": "T4",
      "owner_role": "Reviewer",
      "status": "done",
      "files": [
        "frontend/src/app/network/traffic-policy/page.tsx"
      ],
      "notes": "Manual in-session review completed; no blocking issues found."
    }
  ],
  "commands": {
    "build": "cd frontend && npm run -s build",
    "test": "cd frontend && npx tsc --noEmit --pretty false && SMOKE_BASE_URL='http://localhost:3000' npm run -s smoke:runtime && LD_LIBRARY_PATH=/home/redhot/VyOS/.local-playwright-libs/extracted/usr/lib/x86_64-linux-gnu SMOKE_BASE_URL='http://localhost:3000' npm run -s smoke:ui",
    "lint": "cd frontend && npm run -s lint"
  },
  "worktrees_or_threads": [
    {
      "name": "feature/containers-automation-v1",
      "purpose": "Parity implementation and runtime validation",
      "status": "active"
    }
  ]
}
