{
  "feature_id": "high-availability-vrrp-depth-2026-02-15",
  "title": "High Availability VRRP parity deepening (global + per-group options)",
  "status": "done",
  "acceptance_criteria": [
    "HA page supports VRRP global parameters: startup_delay, version, and global GARP controls.",
    "HA page supports group-level disable, rfc3768-compatibility, excluded-address, and per-group GARP controls.",
    "Save path emits scoped high-availability vrrp set/delete operations with diff-based behavior.",
    "Frontend validation passes: tsc, lint (0 errors), build, runtime smoke, and browser smoke."
  ],
  "assumptions": [
    "Virtual-server (IPVS) sub-tree under high-availability remains a follow-up slice.",
    "Unrelated dirty files in this worktree are preserved and not modified by this slice.",
    "Reviewer agent may be unavailable due thread cap; manual review is acceptable fallback when logged."
  ],
  "tasks": [
    {
      "id": "T1",
      "owner_role": "Analyst",
      "status": "done",
      "files": [
        "frontend/src/app/network/high-availability/page.tsx"
      ],
      "notes": "Mapped missing VRRP options from guide semantics into concrete UI/config fields."
    },
    {
      "id": "T2",
      "owner_role": "UI",
      "status": "done",
      "files": [
        "frontend/src/app/network/high-availability/page.tsx"
      ],
      "notes": "Implemented global parameters card and extended per-group editor fields."
    },
    {
      "id": "T3",
      "owner_role": "Backend",
      "status": "done",
      "files": [
        "frontend/src/app/network/high-availability/page.tsx"
      ],
      "notes": "No backend changes needed; existing config-tree wrapper/API contracts preserved."
    },
    {
      "id": "T4",
      "owner_role": "Test",
      "status": "done",
      "files": [
        "frontend/src/app/network/high-availability/page.tsx",
        "frontend/scripts/check-runtime.sh",
        "frontend/scripts/smoke-ui.mjs"
      ],
      "notes": "Validated compile/build/runtime/browser smoke; no new automated unit tests added for this UI-only diff engine step."
    },
    {
      "id": "T5",
      "owner_role": "Reviewer",
      "status": "done",
      "files": [
        "frontend/src/app/network/high-availability/page.tsx"
      ],
      "notes": "Manual review completed due reviewer-agent thread-cap failure."
    }
  ],
  "commands": {
    "build": "cd frontend && npm run -s build",
    "test": "cd frontend && npx tsc --noEmit --pretty false && SMOKE_BASE_URL='http://localhost:3000' npm run -s smoke:runtime && LD_LIBRARY_PATH=/home/redhot/VyOS/.local-playwright-libs/extracted/usr/lib/x86_64-linux-gnu SMOKE_BASE_URL='http://localhost:3000' npm run -s smoke:ui",
    "lint": "cd frontend && npm run -s lint"
  },
  "worktrees_or_threads": [
    {
      "name": "feature/containers-automation-v1",
      "purpose": "Parity implementation and validation",
      "status": "active"
    }
  ]
}
