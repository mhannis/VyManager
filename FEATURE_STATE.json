{
  "feature_id": "system-info-remove-temp-r1-2026-02-15",
  "title": "Remove temperature display from System Information card",
  "status": "in_review",
  "acceptance_criteria": [
    "CPU temperature is not shown in the System Information card UI.",
    "Unused thermometer icon import is removed from card component.",
    "Frontend type-check passes after change."
  ],
  "assumptions": [
    "Temperature values may still exist in API payloads; only UI visibility is being changed.",
    "Unrelated dirty files in the working tree are pre-existing and out of scope for this hotfix."
  ],
  "tasks": [
    {
      "id": "T1",
      "owner_role": "UI",
      "status": "done",
      "files": [
        "frontend/src/components/dashboard/SystemInformationCard.tsx"
      ],
      "notes": "Removed temperature badge rendering and Thermometer icon import from System Information card."
    },
    {
      "id": "T2",
      "owner_role": "Test",
      "status": "done",
      "files": [],
      "notes": "Ran frontend type-check successfully."
    }
  ],
  "commands": {
    "build": "cd frontend && npm run -s build",
    "test": "cd frontend && npx tsc --noEmit --pretty false",
    "lint": "cd frontend && npm run -s lint"
  },
  "worktrees_or_threads": [
    {
      "name": "feature/containers-automation-v1",
      "purpose": "System Information card UI polish",
      "status": "active"
    }
  ]
}
