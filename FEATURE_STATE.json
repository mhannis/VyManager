{
  "feature_id": "routing-editor-cleanup-2026-02-15",
  "title": "Routing editor cleanup (remove obsolete command/list components)",
  "status": "done",
  "acceptance_criteria": [
    "Unused `frontend/src/components/routing/ProtocolCommandContent.tsx` is removed.",
    "Unused `frontend/src/components/routing/ProtocolSimpleListEditor.tsx` is removed.",
    "Frontend typecheck and build succeed with no unresolved imports.",
    "Runtime smoke and browser smoke pass after cleanup."
  ],
  "assumptions": [
    "All active routing pages now rely on dedicated form-first components and do not need the removed generic editors.",
    "No backend/API changes are required for this cleanup.",
    "Unrelated dirty files in this worktree remain untouched."
  ],
  "tasks": [
    {
      "id": "T1",
      "owner_role": "UI",
      "status": "done",
      "files": [
        "frontend/src/components/routing/ProtocolCommandContent.tsx",
        "frontend/src/components/routing/ProtocolSimpleListEditor.tsx"
      ],
      "notes": "Removed dead components after confirming zero imports."
    },
    {
      "id": "T2",
      "owner_role": "Test",
      "status": "done",
      "files": [
        "frontend/src/components/routing/ProtocolCommandContent.tsx",
        "frontend/src/components/routing/ProtocolSimpleListEditor.tsx"
      ],
      "notes": "Validated with frontend tsc/build plus runtime and browser smoke checks."
    },
    {
      "id": "T3",
      "owner_role": "Reviewer",
      "status": "done",
      "files": [
        "frontend/src/components/routing/ProtocolCommandContent.tsx",
        "frontend/src/components/routing/ProtocolSimpleListEditor.tsx"
      ],
      "notes": "Manual in-session review completed; no blocking issues found."
    }
  ],
  "commands": {
    "build": "cd frontend && npm run -s build",
    "test": "cd frontend && npx tsc --noEmit --pretty false && SMOKE_BASE_URL='http://localhost:3000' npm run -s smoke:runtime && LD_LIBRARY_PATH=/home/redhot/VyOS/.local-playwright-libs/extracted/usr/lib/x86_64-linux-gnu SMOKE_BASE_URL='http://localhost:3000' npm run -s smoke:ui",
    "lint": "cd frontend && npm run -s lint"
  },
  "worktrees_or_threads": [
    {
      "name": "feature/containers-automation-v1",
      "purpose": "Parity implementation and runtime validation",
      "status": "active"
    }
  ]
}
